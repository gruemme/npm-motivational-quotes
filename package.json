{
  "name": "npm-motivational-quotes",
  "version": "1.0.0",
  "description": "Illustration how to use external libraries in NPM",
  "scripts": {
    "start": "rm -rf .parcel-cache && parcel src/index.html",
    "build": "rm -rf dist/* && parcel build src/index.html --public-url './' --dist-dir dist"
  },
  "keywords": [
    "npm",
    "inspirational-quotes"
  ],
  "author": "https://github.com/gruemme",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "@anilseervi/inspirational-quotes": "^1.0.2",
    "bootstrap": "^5.3.3"
  }
}
